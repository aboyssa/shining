# 🎉 إعداد نظام تسجيل الدخول المحسن

## ✅ الميزات المضافة

### 🔐 نظام تسجيل الدخول المحسن
1. **ربط حساب Discord**: المستخدم يربط حسابه بالديسكورد أولاً
2. **إنشاء كلمة المرور**: للمستخدمين الجدد - إنشاء كلمة مرور مع شروط أمان قوية
3. **تسجيل الدخول بكلمة المرور**: للمستخدمين الموجودين - تسجيل الدخول بكلمة المرور المسجلة
4. **تأكيد كلمة المرور**: إعادة إدخال كلمة المرور للتأكد من صحتها
5. **رسائل خطأ مفصلة**: عرض رسائل خطأ واضحة ومفصلة للمستخدم

### 🛡️ نظام الأمان والصلاحيات
1. **الرتب الأساسية**: جميع المستخدمين الجدد يحصلون على رتبة "Player"
2. **الرتب الإدارية**: Leader, Co-Leader, Manager, Staff
3. **إدارة الصلاحيات**: فقط Leader يمكنه إعطاء الرتب والصلاحيات
4. **حماية لوحة الإدارة**: منع الوصول غير المصرح به للوحة الإدارة
5. **زر العودة**: للأداريين للعودة كلاعبين عاديين

### 🎮 واجهة المستخدم
1. **زر لوحة الإدارة**: يظهر فقط للأداريين في صفحة اللاعب
2. **رسائل التحذير**: عند محاولة الوصول غير المصرح به
3. **التواصل مع الدعم**: زر للتواصل مع الدعم الفني عبر الديسكورد
4. **تصميم محسن**: واجهة مستخدم جميلة وسهلة الاستخدام

## 🚀 كيفية الإعداد

### 1. إعداد المتغيرات البيئية
```bash
# في مجلد backend
cp env.template .env
```

قم بتعديل ملف `.env` وأضف:
```env
DISCORD_CLIENT_ID="your_discord_client_id"
DISCORD_CLIENT_SECRET="your_discord_client_secret"
DISCORD_REDIRECT_URI="http://localhost:5000/auth/callback"
JWT_SECRET="your_jwt_secret"
CLIENT_REDIRECT_URL="http://localhost:8000"
```

### 2. تثبيت التبعيات
```bash
# في مجلد backend
npm install

# في المجلد الرئيسي
npm install
```

### 3. إعداد قاعدة البيانات
```bash
# في مجلد backend
node database.js
```

### 4. إعداد المستخدم Leader
```bash
# في مجلد backend
# قم بتعديل ملف setup-leader.js وأضف Discord ID الخاص بك
node setup-leader.js
```

### 5. تشغيل الخادم
```bash
# في مجلد backend
npm start

# في المجلد الرئيسي (في terminal آخر)
npm run dev
```

## 🔧 إعداد Discord OAuth

### 1. إنشاء تطبيق Discord
1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
2. انقر على "New Application"
3. أدخل اسم التطبيق
4. اذهب إلى "OAuth2" في القائمة الجانبية

### 2. إعداد OAuth2
1. في "Redirects" أضف: `http://localhost:5000/auth/callback`
2. انسخ Client ID و Client Secret
3. أضفهما إلى ملف `.env`

### 3. إعداد Scopes
- `identify` - للحصول على معلومات المستخدم الأساسية
- `email` - للحصول على البريد الإلكتروني (اختياري)

## 🎯 تدفق تسجيل الدخول

### للمستخدمين الجدد:
1. **ربط Discord**: المستخدم يضغط على "تسجيل الدخول بـ Discord"
2. **إنشاء كلمة المرور**: يتم توجيهه لصفحة إنشاء كلمة المرور
3. **تأكيد كلمة المرور**: إعادة إدخال كلمة المرور للتأكد
4. **الانتقال للصفحة المناسبة**: حسب الرتبة (Player أو Admin)

### للمستخدمين الموجودين:
1. **ربط Discord**: المستخدم يضغط على "تسجيل الدخول بـ Discord"
2. **تسجيل الدخول بكلمة المرور**: إدخال كلمة المرور المسجلة
3. **الانتقال للصفحة المناسبة**: حسب الرتبة

## 🛡️ الأمان

### كلمة المرور:
- **الحد الأدنى**: 8 أحرف
- **الأرقام**: رقم واحد على الأقل
- **الرموز**: رمز واحد على الأقل (!@#$%^&*)
- **التشفير**: باستخدام bcrypt مع salt rounds = 12

### الرتب والصلاحيات:
- **Player**: الصلاحيات الأساسية
- **Staff**: إدارة التذاكر والبلاغات
- **Manager**: إدارة الطاقم والإحصائيات
- **Co-Leader**: إدارة الأدوار
- **Leader**: جميع الصلاحيات

## 🔗 روابط مفيدة

- **Discord Developer Portal**: https://discord.com/developers/applications
- **Discord OAuth2 Documentation**: https://discord.com/developers/docs/topics/oauth2

## 📝 ملاحظات مهمة

1. **تغيير Discord ID**: تأكد من تغيير Discord ID في ملف `setup-leader.js`
2. **حماية JWT_SECRET**: استخدم مفتاح JWT قوي وآمن
3. **إعدادات الإنتاج**: غير URLs في الإنتاج
4. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية من قاعدة البيانات

## 🆘 الدعم الفني

في حالة مواجهة أي مشاكل:
1. تحقق من إعدادات Discord OAuth
2. تأكد من صحة المتغيرات البيئية
3. تحقق من سجلات الخادم
4. تواصل مع الدعم الفني عبر الديسكورد

---

**تم تطوير هذا النظام بواسطة فريق Shining** 🌟
