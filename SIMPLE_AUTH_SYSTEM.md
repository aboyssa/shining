# 🔐 نظام تسجيل الدخول البسيط - Discord + كلمة المرور

## 🎯 نظرة عامة

نظام بسيط وفعال يسمح للمستخدمين بربط حساباتهم في Discord، ثم إنشاء أو تسجيل الدخول باستخدام كلمة مرور محلية.

## 🛠️ آلية العمل

### 1. ربط حساب Discord (OAuth2)
- المستخدم يضغط على زر "تسجيل الدخول باستخدام Discord"
- يتم توجيهه إلى صفحة مصادقة Discord (OAuth2)
- بعد الموافقة، يتم جلب معلومات المستخدم الأساسية:
  - `id` (معرف المستخدم في Discord)
  - `username`
  - `avatar`

### 2. فحص المستخدم
- يتم التحقق مما إذا كان المستخدم مسجلًا سابقًا في قاعدة البيانات
- إن كان مسجلاً ومربوطًا بكلمة مرور مسبقًا → يُحوّل مباشرة إلى صفحة تسجيل الدخول
- إن لم يكن لديه كلمة مرور بعد → يُحوّل إلى صفحة إنشاء كلمة مرور

### 3. صفحة واحدة - تسجيل دخول أو إنشاء كلمة مرور
واجهة بسيطة تحتوي على:

#### تبويب "تسجيل الدخول":
- حقل "كلمة المرور"
- زر "تسجيل الدخول"
- رابط "إنشاء كلمة مرور جديدة" (إذا لم يكن لديه كلمة مرور)

#### تبويب "إنشاء كلمة مرور":
- حقل "كلمة المرور الجديدة"
- حقل "تأكيد كلمة المرور"
- شروط كلمة المرور
- زر "إنشاء كلمة المرور"
- رابط "تسجيل الدخول" (إذا كان لديه كلمة مرور)

### 4. صفحة Player
صفحة شخصية بسيطة تظهر للمستخدم المسجل دخوله فقط، تحتوي على:
- صورة Discord
- اسم المستخدم
- معلومات الحساب
- حالة الحساب
- إجراءات سريعة

## 🗃️ تخزين البيانات

### قاعدة البيانات:
```sql
CREATE TABLE users (
  id TEXT PRIMARY KEY,           -- Discord ID
  username TEXT NOT NULL,        -- Discord username
  email TEXT,                    -- Discord email
  passwordHash TEXT,             -- كلمة المرور المشفرة
  role TEXT DEFAULT 'Player',    -- رتبة المستخدم
  avatar TEXT,                   -- Discord avatar
  hasPassword INTEGER DEFAULT 0, -- هل لديه كلمة مرور؟
  createdAt TEXT DEFAULT CURRENT_TIMESTAMP,
  updatedAt TEXT DEFAULT CURRENT_TIMESTAMP,
  lastLogin TEXT,
  isActive INTEGER DEFAULT 1
);
```

## 🎯 تدفق المستخدم

### للمستخدمين الجدد:
1. **ربط Discord** → OAuth2
2. **صفحة إنشاء كلمة المرور** → إنشاء كلمة مرور قوية
3. **صفحة Player** → عرض المعلومات الشخصية

### للمستخدمين الموجودين:
1. **ربط Discord** → OAuth2
2. **صفحة تسجيل الدخول** → إدخال كلمة المرور
3. **صفحة Player** → عرض المعلومات الشخصية

## 🛡️ الأمان

### كلمة المرور:
- **الحد الأدنى**: 8 أحرف
- **الأرقام**: رقم واحد على الأقل
- **الرموز**: رمز واحد على الأقل (!@#$%^&*)
- **التشفير**: bcrypt مع salt rounds = 12

### الرتب:
- **Player**: المستخدمين العاديين
- **Leader**: المدير الأعلى
- **Co-Leader**: المدير المساعد
- **Manager**: المدير
- **Staff**: الموظف

## 🚀 كيفية الإعداد

### 1. إعداد البيئة:
```bash
cd backend
copy env.template .env
# قم بتعديل .env وأضف بيانات Discord OAuth
```

### 2. تثبيت التبعيات:
```bash
# في مجلد backend
npm install

# في المجلد الرئيسي
npm install
```

### 3. إعداد قاعدة البيانات:
```bash
cd backend
npm run setup-db
```

### 4. إعداد المستخدم Leader:
```bash
# قم بتعديل setup-leader.js وأضف Discord ID الخاص بك
npm run setup-leader
```

### 5. تشغيل النظام:
```bash
# Terminal 1 - Backend
cd backend && npm start

# Terminal 2 - Frontend
npm run dev
```

## 📁 الملفات المهمة

### Backend:
- `routes/auth.js` - مسارات المصادقة
- `database.js` - إعداد قاعدة البيانات
- `setup-leader.js` - إعداد المستخدم Leader

### Frontend:
- `pages/PasswordAuthPage.tsx` - صفحة تسجيل الدخول/إنشاء كلمة المرور
- `pages/PlayerPage.tsx` - صفحة اللاعب البسيطة
- `components/auth/OAuthCallback.tsx` - معالج OAuth
- `lib/auth-service.ts` - خدمة المصادقة

## 🎨 الميزات

### ✅ مكتمل:
- ربط Discord OAuth2
- إنشاء كلمة مرور قوية
- تسجيل الدخول بكلمة المرور
- صفحة Player بسيطة
- نظام الرتب والصلاحيات
- رسائل خطأ مفصلة
- تصميم عربي احترافي

### 🔄 قابل للتطوير:
- إضافة المزيد من الصفحات
- نظام التذاكر
- إدارة المستخدمين
- لوحة الإدارة المتقدمة

## 📝 ملاحظات مهمة

1. **Discord OAuth**: تأكد من إعداد Client ID و Client Secret بشكل صحيح
2. **كلمة المرور**: استخدم كلمة مرور قوية وآمنة
3. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية من قاعدة البيانات
4. **الأمان**: لا تشارك Client Secret مع أي شخص

---

**نظام بسيط وفعال جاهز للاستخدام!** 🎉 